---
interface Props {
  title?: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---
<div class:list={[className, '']}>
  <h1 class="mt-8 font-semibold text-black dark:text-white">{title}<slot /></h1>
</div>
