---
import { formatDateShort, formatDateTitle, toISODate } from '@/lib/dates';
import { DID, pdslsUrl } from '@/lib/pds';
import PdslsIcon from '@/components/icons/PdslsIcon.astro';
import AuthorCard from '@/components/AuthorCard.astro';

interface Props {
  entry: {
    id: string;
    data: {
      title: string;
      description?: string;
      address?: string;
      thumbnailUrls: string[];
      thumbnailFullUrls: string[];
      photoCount: number;
      createdAt: string;
      galleryRkey: string;
    }
  }
}

const { entry } = Astro.props;
const { data } = entry;
const thumbs = data.thumbnailUrls ?? [];
const fullUrls = data.thumbnailFullUrls ?? [];
const grainUrl = `https://grain.social/profile/${DID}/gallery/${data.galleryRkey}`;
const date = new Date(data.createdAt);
---
<article class="h-entry" data-pagefind-body>
  <AuthorCard />
  {thumbs.length > 0 && (
    <div class="overflow-x-auto mb-2">
      <div class="flex gap-1">
        {thumbs.map((url, i) => (
          <a href={fullUrls[i]} rel="noopener noreferrer" class="flex-none">
            <img
              src={url}
              alt={data.title}
              width="120"
              height="120"
              class:list={["u-photo size-30 aspect-square object-cover",
                i === 0 && "rounded-l-xl",
                i === thumbs.length - 1 && "rounded-r-xl"
              ]}
              loading="lazy"
              decoding="async"
            />
          </a>
        ))}
      </div>
    </div>
  )}
  <div>
    <a class="p-name u-url underline inline-flex items-center gap-1" href={grainUrl} rel="noopener noreferrer">
      {data.title}
    </a>
    {data.description && <p class="e-content text-sm text-gray-600 dark:text-gray-400 mt-0.5">{data.description}</p>}
    <div class="text-gray-500 dark:text-gray-400">
      <time class="dt-published text-sm mr-1" datetime={toISODate(date)} title={formatDateTitle(date)}>{formatDateShort(date)}</time>
      <a class="underline" href={pdslsUrl(`at://${DID}/social.grain.gallery/${data.galleryRkey}`)} rel="noopener noreferrer" aria-label="View on PDSls"><PdslsIcon /></a>
    </div>
  </div>
</article>
